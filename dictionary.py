# dictionary are to used to store data in key value pair they are not unorderd, 
# mutable and don't allow duplicate keys

# calling values using keys
# error when we call any undefine key
# don't allow duplicate keys
# null dictionary
# methods

a['name']
a.get('name')